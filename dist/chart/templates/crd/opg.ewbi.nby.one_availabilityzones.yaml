{{- if .Values.crd.enable }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.crd.keep }}
    "helm.sh/resource-policy": keep
    {{- end }}
    controller-gen.kubebuilder.io/version: v0.16.5
  name: availabilityzones.opg.ewbi.nby.one
spec:
  group: opg.ewbi.nby.one
  names:
    kind: AvailabilityZone
    listKind: AvailabilityZoneList
    plural: availabilityzones
    shortNames:
    - az
    singular: availabilityzone
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: AvailabilityZone is the Schema for the availabilityzones API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AvailabilityZoneSpec defines the desired state of AvailabilityZone.
            properties:
              geographyDetails:
                description: |-
                  GeographyDetails Details about cities or state covered by the edge.
                  Details about the type of locality for eg rural, urban, industrial etc.
                  This information is defined in human readable form.
                type: string
              geolocation:
                description: Geolocation Latitude,Longitude as decimal fraction up
                  to 4 digit precision
                type: string
              zoneId:
                description: ZoneId Human readable name of the zone.
                type: string
            type: object
          status:
            description: AvailabilityZoneStatus defines the observed state of AvailabilityZone.
            properties:
              computeResourceQuotaLimits:
                description: To be considered, for now these are just placeholder
                  samples
                type: string
              flavoursSupported:
                description: To be considered, for now these are just placeholder
                  samples
                items:
                  type: string
                type: array
              latency:
                description: To be considered, for now these are just placeholder
                  samples
                type: string
              phase:
                type: string
              reservedComputeResources:
                description: To be considered, for now these are just placeholder
                  samples
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end -}}
